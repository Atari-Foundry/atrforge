name: Release

# This workflow is disabled - releases are created locally using 'make github-release'
# The workflow: make github-release pushes to develop, waits for CI, then builds locally and creates releases
# See CONTRIBUTING.md and Makefile for release instructions

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled. Use "make github-release" locally instead.'
        required: false
        type: string

jobs:
  disabled:
    name: Release Workflow Disabled
    runs-on: ubuntu-latest
    steps:
    - name: Notice
      run: |
        echo "⚠️  This workflow is disabled."
        echo ""
        echo "Releases are created locally using: make github-release"
        echo ""
        echo "The release process:"
        echo "  1. make github-release pushes to develop branch"
        echo "  2. Waits for CI to pass on develop"
        echo "  3. Builds all platforms locally"
        echo "  4. Creates GitHub release locally"
        echo ""
        echo "See CONTRIBUTING.md for more information."
